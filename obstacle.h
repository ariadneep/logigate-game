#ifndef OBSTACLE_H
#define OBSTACLE_H

#include "gridcomponent.h"

class Obstacle : public GridComponent {

    public:
        explicit Obstacle(QObject *parent = nullptr);

};

#endif // OBSTACLE_H
